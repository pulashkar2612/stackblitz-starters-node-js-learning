const addFunction = (x, y) => {
  return x + y;
};

const multiplyFunction = (x, y) => {
  return x * y;
};

const newVariable = 15;

module.exports = { addFunction, multiplyFunction, newVariable };
